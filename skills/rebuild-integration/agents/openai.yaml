interface:
  display_name: "Rebuild Integration"
  short_description: "Rebuild integration/latest from the stack"
  default_prompt: "Use $rebuild-integration to reconstruct integration/latest from the active features."

policy:
  allow_implicit_invocation: false
