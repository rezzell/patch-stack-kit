interface:
  display_name: "Rebuild Integration"
  short_description: "Rebuild the manifest finish branch"
  default_prompt: "Use $rebuild-integration to reconstruct the manifest finish branch from the active features."

policy:
  allow_implicit_invocation: false
