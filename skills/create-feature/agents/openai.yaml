interface:
  display_name: "Create Feature"
  short_description: "Create a tracked feature branch"
  default_prompt: "Use $create-feature to create a tracked feature branch and manifest entry."

policy:
  allow_implicit_invocation: false
