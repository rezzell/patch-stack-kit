interface:
  display_name: "Sync Upstream"
  short_description: "Fast-forward the manifest start branch"
  default_prompt: "Use $sync-upstream to fast-forward the manifest start branch from upstream and origin."

policy:
  allow_implicit_invocation: false
